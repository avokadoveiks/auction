# Система Ботов для Аукционной Игры

## 📁 Структура файлов

```
src/
├── config/
│   └── bots.config.json      # Конфигурация поведения ботов
├── core/
│   └── Events.js             # Система событий (Observer pattern)
├── bots/
│   ├── BotArchetypes.js      # Архетипы поведения (Turtle)
│   ├── BotRunner.js          # Планировщик ставок ботов
│   └── BotDirector.js        # Дирижёр интенсивности
├── fx/
│   ├── coins.js              # Модуль эффектов монет
│   └── winEffects.js         # Автоматические эффекты победы
└── index.js                  # Интеграция с основной игрой
```

## 🎮 Как это работает

### 1. Конфигурация (`bots.config.json`)

```json
{
  "enabled": true,              // Вкл/выкл систему
  "targetRPM": 18,              // Целевая частота ставок в минуту
  "perColumn": { 
    "minRPM": 2, 
    "maxRPM": 5 
  },
  "cooldowns": { 
    "afterBidMs": 1800          // Задержка после ставки (1.8 сек)
  },
  "limits": { 
    "maxConsecutiveOverbidSamePlayer": 2  // Анти-токсичность
  },
  "economy": { 
    "minBalance": 30,           // Минимальный баланс бота
    "maxBalance": 120,          // Максимальный баланс бота
    "refillPerRound": 10        // Пополнение после раунда
  },
  "archetypeWeights": { 
    "Turtle": 1.0               // Вес архетипа (пока только Turtle)
  }
}
```

### 2. Архетипы (`BotArchetypes.js`)

**Turtle** - Осторожный игрок:
- Медленные реакции: 2.2-4.8 секунд
- Низкая вероятность ставки: 25%
- Экономный: оставляет резерв минимум 6

### 3. Раннер (`BotRunner.js`)

Управляет отдельными ботами:
- Инициализирует ботов со случайным балансом (30-120)
- Планирует ставки с задержками
- Проверяет условия:
  - ✅ Кулдаун (1800мс)
  - ✅ Случайность (bidChance)
  - ✅ Достаточный баланс
  - ✅ Лимит перебиваний одного игрока
- Пополняет балансы после раундов

### 4. Директор (`BotDirector.js`)

Управляет интенсивностью:
- Создаёт 3 бота-черепахи
- Адаптирует активность к количеству игроков:
  - `roomTargetRPM = targetRPM - humans * 2`
  - Минимум 4 RPM
- Распределяет ставки вероятностно для естественности

### 5. Интеграция (`src/index.js`)

Связывает систему ботов с игрой:
- Загружает конфигурацию
- Создаёт директора
- Подключает к функциям игры:
  - `makeBet()` - размещение ставок
  - `columnState` - состояние колонок
  - `COLUMNS` - конфигурация колонок
  - `ACCOUNTS` - игроки

## 🎬 Автоматические эффекты победы

### Как это работает

При победе любого игрока (человека или бота):
1. Вызывается `emitRoundEnd({ columnId, winnerId, isBot, prize })`
2. Обработчик в `winEffects.js` автоматически запускается
3. Определяется цель для анимации:
   - **Если победитель — текущий игрок**: монеты летят в `#currentBalance` (16 частиц)
   - **Если победил другой игрок/бот**: монеты летят в его кошелёк или pot колонки (12 частиц)
4. Используется существующий `flyCoins` из v1.js

### Приоритет поиска цели

Для чужих побед (включая ботов):
1. `#player-{winnerId} .wallet` — кошелёк победителя
2. `#player-{winnerId} [data-wallet]` — альтернативный селектор
3. `.column[data-column="{columnId}"] .pot` — pot колонки
4. `#spectator-pot` — резервный якорь (невидимый)
5. `#balance` / `#currentBalance` — последний резерв

### Интеграция

Эффекты **не меняют логику начисления денег** — только визуализация.
Реальные балансы обновляются как раньше в v1.js.

## 🚀 Использование

### Автоматический запуск

Система запускается автоматически при загрузке страницы (см. `v1.html`).
Эффекты монет также инициализируются автоматически.

### Ручное управление

```javascript
// Остановить ботов
window.botSystem.stop();

// Запустить заново
window.botDirector.start();

// Вручную вызвать пополнение балансов
window.botSystem.onRoundEnd();
```

### Отладка

В консоли доступны:
- `window.botDirector` - экземпляр директора
- `window.botSystem` - управление системой
- `window.columnState` - состояние колонок
- `window.ACCOUNTS` - все игроки и боты

## ⚙️ Настройка

### Изменить количество ботов

В `src/index.js`, строка 31:
```javascript
botDirector.spawnBots(3); // Изменить 3 на нужное число
```

### Изменить частоту ставок

В `src/config/bots.config.json`:
```json
"targetRPM": 18  // Изменить на нужное значение
```

### Отключить систему

В `src/config/bots.config.json`:
```json
"enabled": false
```

## 📊 Алгоритм работы

```
Каждую секунду (tick):
1. Получить количество живых игроков (humans)
2. Вычислить targetRPM = max(4, configRPM - humans*2)
3. Перевести в вероятность: perSecTarget = targetRPM / 60
4. С вероятностью perSecTarget:
   - Выбрать случайную колонку
   - Выбрать случайного бота
   - Запланировать ставку с задержкой 2.2-4.8 сек
5. При планировании ставки:
   - Проверить кулдаун (1.8 сек)
   - Проверить случайность (25% для Turtle)
   - Проверить баланс (резерв >= 6)
   - Проверить лимит перебиваний
   - Сделать ставку
```

## 🔧 Расширение системы

### Добавить новый архетип

В `src/bots/BotArchetypes.js`:
```javascript
export function makeAggressive() {
  return {
    name: "Aggressive",
    baseDelayMsRange: [800, 1500],  // Быстрые реакции
    bidChance: 0.8,                 // Часто ставит
    minReserve: 2                   // Рискованный
  };
}
```

В `src/bots/BotDirector.js`:
```javascript
import { makeTurtle, makeAggressive } from "./BotArchetypes.js";

// В конструкторе:
this.archetypes = [makeTurtle(), makeAggressive()];
```

### Добавить события

В `src/core/Events.js` уже есть система событий. Можно подписаться:
```javascript
import { onBidPlaced, emitBidPlaced } from './src/core/Events.js';

onBidPlaced(({ columnId, playerId, amount }) => {
  console.log(`Ставка: ${playerId} -> ${amount} на колонку ${columnId}`);
});
```

## 📝 История изменений

### v1.1.0 (2025-10-23)
- ✅ **Автоматические эффекты монет при победе**
  - Эффект срабатывает для любого победителя (человек/бот)
  - Умный выбор цели: баланс игрока / кошелёк / pot колонки
  - Резервный якорь #spectator-pot
- ✅ Система событий полностью интегрирована
- ✅ Модульная архитектура эффектов

### v1.0.0 (2025-10-23)
- ✅ Базовая система с архетипом Turtle
- ✅ Адаптивная интенсивность (зависит от игроков)
- ✅ Анти-токсичность (лимит перебиваний)
- ✅ Экономика ботов (баланс, пополнение)
- ✅ Интеграция с основной игрой

## 🐛 Известные ограничения

1. Боты используют функцию `makeBet()` которая проверяет текущего игрока - нужна адаптация
2. Пока поддерживается только один архетип (Turtle)
3. Система событий создана, но пока не используется полностью

## 🎯 Планы развития

- [ ] Добавить архетипы: Aggressive, Balanced, Random
- [ ] Улучшить логику выбора колонки (учитывать heat, банк, таймер)
- [ ] Добавить "личности" ботам (предпочтения колонок)
- [ ] Интегрировать систему событий полностью
- [ ] Добавить аналитику активности ботов
