# Dev Server - Режим разработки

## 🚀 Что изменилось

Теперь запущен **специальный dev-сервер** с отключённым кэшированием!

## ✅ Как это работает

1. **Кэширование отключено полностью** - сервер отправляет заголовки:
   - `Cache-Control: no-store, no-cache, must-revalidate`
   - `Pragma: no-cache`
   - `Expires: 0`

2. **Изменения видны сразу** после обычной перезагрузки (`Cmd+R` / `Ctrl+R`)

3. **Не нужно**:
   - ❌ Жёсткая перезагрузка (Cmd+Shift+R)
   - ❌ Очистка кэша браузера
   - ❌ Перезапуск сервера
   - ❌ Изменение версий в URL (?v=...)

## 📋 Использование

### Запуск сервера
```bash
./dev-start.sh
```

Или вручную:
```bash
python3 dev-server.py
```

### Остановка сервера
```bash
./stop_server.sh
```

Или:
```bash
kill $(cat server.pid)
```

### Проверка статуса
```bash
ps -p $(cat server.pid)
```

## 🌐 URL для доступа

- **Локально**: http://localhost:5500
- **LAN (iPhone)**: http://192.168.1.232:5500

## 💡 Workflow для разработки

1. Запустите dev-сервер один раз
2. Откройте сайт в браузере
3. Редактируйте CSS/JS/HTML файлы
4. Просто нажмите **Cmd+R** (или F5) для обновления
5. Изменения применятся сразу!

## 🔧 Доступные серверы

### `dev-server.py` (текущий)
- ✅ Отключённое кэширование
- ✅ Простой и быстрый
- ℹ️ Нужна ручная перезагрузка браузера

### `dev-server-livereload.py` (экспериментальный)
- ✅ Отключённое кэширование
- ✅ Автоматическая перезагрузка при изменениях
- ℹ️ Использует Server-Sent Events
- ℹ️ Требует добавления скрипта в HTML

## 📝 Файлы

- `dev-server.py` - основной dev-сервер
- `dev-server-livereload.py` - с автоматической перезагрузкой
- `dev-start.sh` - скрипт запуска
- `server.pid` - PID текущего сервера
- `dev-server.log` - логи сервера
